import { ScrollView } from "react-native";
import { Notificacao } from "../components/paginaNotificacao/notificacao";

export const NotificationScreen = () => {
  return (
    <ScrollView>
      <Notificacao />
    </ScrollView>
  );
};
